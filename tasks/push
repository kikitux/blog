cd _site
pwd
echo "push of _site to kikitux.github.com" | growlnotify
git add . && git commit -a -m "update kikitux site" && git push
cd ..
pwd
echo "push of kikitux.net to mini" | growlnotify
git add . && git commit -a -m "update kikitux site" && git push
